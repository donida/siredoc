<%- model_class = Cidade -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:nome) %></th>
      <th><%= model_class.human_attribute_name(:estado_id) %></th>
      <th><%= model_class.human_attribute_name(:comarca_id) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @cidades.each do |cidade| %>
      <tr>
        <td><%= link_to cidade.id, cidade_path(cidade) %></td>
        <td><%= cidade.nome %></td>
        <td><%= cidade.estado.nome %></td>
        <td><%= cidade.comarca_id == nil ? 'Nao Cadastrado' : cidade.comarca.nome  %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_cidade_path(cidade), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      cidade_path(cidade),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
  <div class="form-actions">
    	<%= will_paginate @noticias %>
  </div>
<%= link_to t('.new', :default => t("helpers.links.new")),
            new_cidade_path,
            :class => 'btn btn-primary' %>
